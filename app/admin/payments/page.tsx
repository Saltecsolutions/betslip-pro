'use client';
import {FormEvent,useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import {useAdmin} from '@/lib/use-admin';
import {Empty,Money,PageHeading,Status,useLanguage} from '@/components/ui';
export default function Payments(){const {db,ready}=useAdmin();const {t}=useLanguage();const [rows,setRows]=useState<any[]>([]);const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function load(){const {data,error}=await db.rpc('admin_compliance_read',{p_area:'payments',p_reason:'Payment reconciliation workspace'});setRows(data||[]);if(error)setMessage(error.message)}useEffect(()=>{if(ready)void load()},[ready]);const totals=useMemo(()=>rows.reduce((a,p)=>{const gross=Number(p.amount_tzs||0);const platform=Number(p.platform_commission_tzs??gross*.30);const tipster=Number(p.tipster_commission_tzs??gross*.70);const wht=gross*.05;const platformNet=Math.max(0,platform-wht);const fee=Number(p.processing_fee_tzs||0);a.gross+=gross;a.tipster+=tipster;a.platform+=platform;a.wht+=wht;a.platformNet+=platformNet;a.fees+=fee;return a},{gross:0,tipster:0,platform:0,wht:0,platformNet:0,fees:0}),[rows]);async function verify(e:FormEvent<HTMLFormElement>,id:string){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const {error}=await db.rpc('admin_verify_manual_payment',{p_purchase_id:id,p_processing_fee_tzs:Number(f.get('fee'))});setMessage(error?error.message:t('Payment verified.','Malipo yamethibitishwa.'));if(!error)await load();setBusy(false)}if(!ready)return <main className="container"><div className="skeleton"/></main>;return <main className="container"><PageHeading eyebrow={t('PAYMENT OPERATIONS','USIMAMIZI WA MALIPO')} title={t('Revenue, tax and payouts in one view.','Mapato, kodi na malipo sehemu moja.')} description={t('See the full 70/30 marketplace split. The 5% withholding-tax allocation is tracked inside Betslip Pro’s 30% share, leaving a 25% platform net share before processing costs.','Angalia mgawanyo kamili wa 70/30. Allocation ya withholding tax ya 5% inafuatiliwa ndani ya 30% ya Betslip Pro, hivyo platform hubaki na 25% kabla ya ada za miamala.')}/><Link className="btn" href="/admin">← {t('Admin workspace','Sehemu ya admin')}</Link>{message&&<p className="notice" role="status">{message}</p>}<div className="stats-grid" style={{marginTop:24}}>{[[t('Gross sales','Mauzo yote'),totals.gross],[t('Tipsters · 70%','Tipsters · 70%'),totals.tipster],[t('Platform gross · 30%','Platform gross · 30%'),totals.platform],[t('WHT allocation · 5%','WHT allocation · 5%'),totals.wht],[t('Platform net · 25%','Platform net · 25%'),totals.platformNet],[t('Processing fees','Ada za miamala'),totals.fees]].map(([label,value])=><div className="card" key={String(label)}><span className="stat-label">{label}</span><b className="stat-value"><Money amount={Number(value)}/></b></div>)}</div><p className="compact-copy">{t('Tax allocation is shown separately for transparency and should remain configurable if TRA treatment changes or is confirmed differently for a specific transaction.','Allocation ya kodi inaonyeshwa tofauti kwa uwazi na itaendelea kuwa configurable endapo treatment ya TRA itabadilika au kuthibitishwa tofauti kwa muamala fulani.')}</p><section style={{marginTop:24}}>{rows.length?<div className="grid">{rows.map(p=>{const gross=Number(p.amount_tzs||0);const platform=Number(p.platform_commission_tzs??gross*.30);const tipster=Number(p.tipster_commission_tzs??gross*.70);const wht=gross*.05;const platformNet=Math.max(0,platform-wht);return <article className="card" key={p.id}><Status value={p.payment_status}/><h2 style={{marginTop:18}}><Money amount={gross}/></h2><p>{t('Reference','Kumbukumbu')}: <b>{p.payment_reference||'—'}</b></p><div className="data-row"><span className="compact-copy">Tipster · 70%</span><Money amount={tipster}/></div><div className="data-row"><span className="compact-copy">Betslip Pro gross · 30%</span><Money amount={platform}/></div><div className="data-row"><span className="compact-copy">WHT allocation · 5%</span><Money amount={wht}/></div><div className="data-row"><span className="compact-copy">Betslip Pro net · 25%</span><Money amount={platformNet}/></div>{p.payment_status==='submitted'?<form onSubmit={e=>void verify(e,p.id)}><label>{t('Processing fee · TZS','Ada ya muamala · TZS')}<input name="fee" type="number" step="1" min="0" defaultValue="0" required/></label><label className="settings-row"><input type="checkbox" required/>{t('I reconciled the reference and amount.','Nimehakiki kumbukumbu na kiasi.')}</label><button className="btn btn-primary" disabled={busy}>{t('Verify payment','Thibitisha malipo')}</button></form>:<p className="compact-copy">{t('Processing fee','Ada ya muamala')}: <Money amount={p.processing_fee_tzs}/></p>}</article>})}</div>:<Empty icon="shield" title={t('Payments are up to date.','Hakuna malipo yanayosubiri.')} description={t('Submitted payments will appear here for verification.','Malipo yaliyowasilishwa yataonekana hapa kwa uthibitisho.')}/>}</section></main>}
