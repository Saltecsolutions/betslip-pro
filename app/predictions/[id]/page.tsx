'use client';
import {useEffect,useMemo,useRef,useState} from 'react';
import {useParams,useRouter} from 'next/navigation';
import Link from 'next/link';
import {createClient} from '@/lib/supabase/client';
import {Avatar,Empty,Icon,Money,Status,useLanguage} from '@/components/ui';
import {Slip} from '@/components/marketplace';
export default function Detail(){const {id}=useParams<{id:string}>();const router=useRouter();const db=useMemo(()=>createClient(),[]);const {t,locale}=useLanguage();const [slip,setSlip]=useState<Slip|null>(null);const [content,setContent]=useState<{prediction_text:string;betslip_code:string}|null>(null);const [loaded,setLoaded]=useState(false);const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const auto=useRef(false);useEffect(()=>{void(async()=>{const {data,error}=await db.from('predictions').select('id,tipster_id,title,sport,league,odds,selection_count,price_tzs,match_date,result,tipsters(display_name,profile_image_url,verification_status)').eq('id',id).single();setSlip(data as unknown as Slip);if(error)setMessage(t('This prediction could not be loaded.','Utabiri huu haujapakia.'));const {data:auth}=await db.auth.getUser();if(auth.user){const {data:paid}=await db.rpc('get_prediction_protected_content',{p_prediction_id:id});setContent(paid?.[0]||null)}setLoaded(true)})()},[id,db]);async function buy(){if(busy||!slip)return;setBusy(true);setMessage('');try{const {data:{user}}=await db.auth.getUser();const next=`/predictions/${id}?buy=1`;if(!user){router.push(`/login?next=${encodeURIComponent(next)}`);return}const {data:profile}=await db.from('profiles').select('age_verified').eq('id',user.id).single();if(!profile?.age_verified){router.push(`/account/confirm-age?next=${encodeURIComponent(next)}`);return}const {data,error}=await db.rpc('create_purchase',{p_prediction_id:id});if(error)throw error;router.push(`/purchases/${data}/payment`)}catch(e){setMessage(e instanceof Error?e.message:t('Purchase could not be created. Please retry.','Ununuzi haujakamilika. Jaribu tena.'))}finally{setBusy(false)}}useEffect(()=>{if(loaded&&slip&&!content&&!auto.current&&new URLSearchParams(window.location.search).get('buy')==='1'){auto.current=true;void buy()}},[loaded,slip,content]);if(!loaded)return <main className="container"><div className="skeleton"/></main>;if(!slip)return <main className="container"><Empty title={t('Slip unavailable','Slip haipatikani')} description={message}><Link href="/predictions" className="btn">{t('Back to marketplace','Rudi sokoni')}</Link></Empty></main>;const started=new Date(slip.match_date)<=new Date();return <main className="container"><Link className="compact-copy" href="/predictions">← {t('Marketplace','Soko')}</Link><div className="page-heading"><div><span className="badge"><Icon name="ball" size={14}/>{slip.sport}</span><h1>{slip.title}</h1></div><Status value={slip.result}/></div><div className="split-layout"><section className="panel"><Link href={`/tipsters/${slip.tipster_id}`} className="person"><Avatar name={slip.tipsters?.display_name||'Tipster'} url={slip.tipsters?.profile_image_url}/><div><strong>{slip.tipsters?.display_name}</strong><small>{t('View the complete track record','Angalia rekodi kamili')} ↗</small></div><span className="positive"><Icon name="shield"/></span></Link><div className="stats-grid" style={{gridTemplateColumns:'repeat(2,1fr)'}}><div><span className="stat-label">{t('Selections','Chaguo')}</span><b className="stat-value">{slip.selection_count}</b></div><div><span className="stat-label">{t('Total odds','Jumla ya odds')}</span><b className="stat-value positive">{slip.odds?.toFixed(2)??'—'}</b></div></div><p className="slip-meta"><Icon name="clock"/>{new Date(slip.match_date).toLocaleString(locale==='sw'?'sw-TZ':'en-GB',{timeZone:'Africa/Dar_es_Salaam'})} EAT</p>{content?<div className="locked-preview" style={{textAlign:'left'}}><span className="badge"><Icon name="check" size={14}/>{t('Unlocked','Imefunguliwa')}</span><h2 style={{marginTop:20}}>{t('Your prediction','Utabiri wako')}</h2><p style={{whiteSpace:'pre-wrap'}}>{content.prediction_text}</p><label>{t('Betslip code','Code ya betslip')}</label><code>{content.betslip_code}</code></div>:<div className="locked-preview"><Icon name="lock" size={30}/><h3>{t('The full play. After purchase.','Utabiri kamili. Baada ya kununua.')}</h3><p className="compact-copy">{t('Teams, selections, analysis and the betslip code stay private until your payment is verified.','Timu, chaguo, uchambuzi na code ya betslip hubaki siri hadi malipo yako yathibitishwe.')}</p><div className="locked-lines" aria-hidden="true">{Array.from({length:Math.min(slip.selection_count,4)},(_,i)=><div className="locked-line" key={i}><Icon name="lock" size={14}/><span/><b/></div>)}</div></div>}</section><aside className="stack" style={{alignContent:'start'}}><div className="panel"><span className="stat-label">{t('ONE-TIME PURCHASE','UNUNUZI WA MARA MOJA')}</span><h2><Money amount={slip.price_tzs}/></h2><p className="compact-copy">{t('Pay via Selcom. Your slip unlocks after an admin verifies the payment.','Lipa kupitia Selcom. Slip hufunguka baada ya admin kuthibitisha malipo.')}</p>{!content&&<button disabled={busy||started} className="btn btn-primary" style={{width:'100%'}} onClick={()=>void buy()}>{busy?t('Please wait…','Subiri…'):started?t('Sales closed','Mauzo yamefungwa'):t('Buy this prediction','Nunua utabiri huu')}<Icon name="arrow" size={16}/></button>}{message&&<p className="notice" role="status">{message}</p>}<p className="notice">{t('18+ · Predictions are opinions, not guaranteed outcomes.','18+ · Utabiri ni maoni, si uhakika wa matokeo.')}</p></div><Link href="/protection" className="card"><span className="positive"><Icon name="shield"/></span><h3 style={{marginTop:16}}>{t('Buyer protection','Ulinzi wa mnunuzi')}</h3><p className="compact-copy">{t('Late delivery or content that doesn’t match the preview? Request a review from your purchases. Losing bets alone are not refundable.','Utabiri uliochelewa au maudhui yasiyolingana? Omba ukaguzi kupitia ununuzi wako. Kupoteza dau pekee si sababu ya kurejeshewa malipo.')}</p><span className="positive">{t('How it works','Jinsi inavyofanya kazi')} ↗</span></Link></aside></div></main>}
