'use client';
import {useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import {createClient} from '@/lib/supabase/client';
import {Empty,Icon,Money,PageHeading,Status,useLanguage} from '@/components/ui';
export default function Tipster(){const db=useMemo(()=>createClient(),[]);const {t}=useLanguage();const [wallet,setWallet]=useState<any>(null);const [summary,setSummary]=useState<any>(null);const [tipster,setTipster]=useState<any>(null);const [payouts,setPayouts]=useState<any[]>([]);const [predictions,setPredictions]=useState<any[]>([]);const [error,setError]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const {data:{user}}=await db.auth.getUser();if(!user){window.location.assign('/login?next=/tipster');return}const {data:tip}=await db.from('tipsters').select('id,verification_status').eq('user_id',user.id).maybeSingle();setTipster(tip);if(tip){const [w,s,p,pr]=await Promise.all([db.from('wallets').select('*').eq('user_id',user.id).single(),db.rpc('tipster_business_summary'),db.from('withdrawals').select('id,amount_tzs,status,created_at').eq('tipster_id',tip.id).order('created_at',{ascending:false}),db.from('predictions').select('id,title,result,status,match_date').eq('tipster_id',tip.id).order('created_at',{ascending:false})]);setWallet(w.data);setSummary(s.data);setPayouts(p.data||[]);setPredictions(pr.data||[]);if(w.error||s.error||p.error||pr.error)setError(t('Some business data could not be loaded. Refresh to retry.','Baadhi ya taarifa hazijapakia. Pakia ukurasa tena.'))}setLoading(false)})()},[db]);if(loading)return <main className="container"><div className="skeleton"/></main>;if(!tipster)return <main className="container"><Empty icon="user" title={t('Tipster account required','Akaunti ya tipster inahitajika')} description={t('Your account has no tipster application.','Akaunti yako haina ombi la tipster.')}><Link className="btn" href="/dashboard">{t('My account','Akaunti yangu')}</Link></Empty></main>;return <main className="container"><PageHeading eyebrow={t('TIPSTER STUDIO','STUDIO YA TIPSTER')} title={t('Your expertise. Your business.','Utaalamu wako. Biashara yako.')} action={<div className="actions"><Link className="btn btn-secondary" href="/tipster/profile">{t('Edit profile','Hariri wasifu')}</Link>{tipster.verification_status==='active'&&<Link className="btn btn-primary" href="/tipster/predictions/new"><Icon name="plus"/>{t('New prediction','Utabiri mpya')}</Link>}</div>}/><Status value={tipster.verification_status}/>{error&&<p role="alert" className="notice">{error}</p>}<div className="stats-grid">{[[t('Verified sales','Mauzo yaliyothibitishwa'),summary?.sales,false],[t('Gross revenue','Mapato ghafi'),summary?.gross,true],[t('Betslip Pro · 30%','Betslip Pro · 30%'),summary?.platform,true],[t('Your earnings · 70%','Mapato yako · 70%'),summary?.earnings,true]].map(([label,value,money])=><div className="card" key={String(label)}><span className="stat-label">{label}</span><b className="stat-value">{value==null?'—':money?<Money amount={Number(value)}/>:value}</b></div>)}</div><div className="feature-banner"><div><h3>{t('A clear split. Every sale.','Mgawanyo wazi. Kila mauzo.')}</h3><p className="muted">{t('Processing fees are recorded separately and do not change the 30% / 70% split.','Ada za miamala zinarekodiwa tofauti na hazibadili mgawanyo wa 30% / 70%.')}</p></div><div><span className="stat-label">{t('Processing fees','Ada za miamala')}</span><b>{summary?<Money amount={summary.fees}/>:'—'}</b></div></div><div className="stats-grid" style={{gridTemplateColumns:'repeat(3,minmax(0,1fr))'}}>{[[t('Pending balance','Salio linalosubiri'),wallet?.pending_balance_tzs],[t('Available balance','Salio linalopatikana'),wallet?.available_balance_tzs],[t('Paid out','Yaliyolipwa'),wallet?.total_withdrawn_tzs]].map(([label,value])=><div className="card" key={label}><span className="stat-label">{label}</span><b className="stat-value">{value==null?'—':<Money amount={value}/>}</b></div>)}</div><section className="panel"><h2>{t('Your predictions','Utabiri wako')}</h2>{predictions.length?predictions.map(p=><div className="data-row" key={p.id}><Link href={`/predictions/${p.id}`}>{p.title}</Link><div><Status value={p.status}/><Status value={p.result}/></div></div>):<Empty title={t('Make your first call','Weka utabiri wako wa kwanza')} description={t('Approved predictions build your public track record. Published content cannot be changed.','Utabiri ulioidhinishwa hujenga rekodi yako. Maudhui yaliyochapishwa hayawezi kubadilishwa.')}/>}</section><section className="panel"><h2>{t('Payout history','Historia ya malipo')}</h2><p className="compact-copy">{t('Payouts are handled and reconciled manually by the Betslip Pro team. Available balance does not mean a transfer has been sent.','Malipo hushughulikiwa na kuhakikiwa na timu ya Betslip Pro. Salio linalopatikana halimaanishi fedha zimetumwa.')}</p>{payouts.length?payouts.map(p=><div className="data-row" key={p.id}><Money amount={p.amount_tzs}/><Status value={p.status}/></div>):<Empty title={t('No payouts yet','Bado hakuna malipo')} description={t('Your payout records will appear here once processed.','Rekodi za malipo zitaonekana hapa baada ya kushughulikiwa.')}/>}</section></main>}
