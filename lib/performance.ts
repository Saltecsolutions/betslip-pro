export type Expert={id:string;display_name:string;bio:string|null;profile_image_url:string|null;verification_status:string;created_at:string;total_predictions:number;settled:number;wins:number;losses:number;voids:number;win_rate:number|null;roi:number|null;profit:number;settled_30d:number;roi_30d:number|null;streak:number;followers:number;sales_30d:number};
export const rankingKeys=['trending','profit','roi','consistent','new'] as const;
export type Ranking=typeof rankingKeys[number];
export const rankingLabels:Record<Ranking,[string,string]>={trending:['Trending','Maarufu sasa'],profit:['Most Profitable','Faida zaidi'],roi:['Best ROI','ROI bora'],consistent:['Most Consistent','Uthabiti zaidi'],new:['New & Rising','Wapya wanaochipuka']};
export function rankExperts(experts:Expert[],rank:Ranking){return [...experts].filter(e=>rank==='trending'||rank==='new'&&new Date(e.created_at).getTime()>Date.now()-90*86400000||rank!=='new'&&e.settled>=20).sort((a,b)=>{if(rank==='new')return b.settled-a.settled;if(rank==='trending')return b.sales_30d-a.sales_30d||b.followers-a.followers;if(rank==='profit')return b.profit-a.profit;if(rank==='roi')return (b.roi??-Infinity)-(a.roi??-Infinity);const score=(e:Expert)=>{const n=Number(e.settled),p=Number(e.wins)/n,z=1.96;return (p+z*z/(2*n)-z*Math.sqrt((p*(1-p)+z*z/(4*n))/n))/(1+z*z/n)};return score(b)-score(a)})}
